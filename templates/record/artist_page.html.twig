{% extends '_template.html.twig' %}

{% block title %}Détails des artistes!{% endblock %}

{% block content %}

<div class="section">
    <div class="title">
    <h1>{{artist.name}}</h1>
    <p>{{artist.description}}</p>

    <h3 class="subtititle">
        Discographie : {{artist.records|length}}
    </h3>
    {% include 'record/_record_list.html.twig' with {record_list:artist.records} %}
    </div>
</div>
{% endblock %}
